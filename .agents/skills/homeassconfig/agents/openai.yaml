interface:
  display_name: "Homeassconfig"
  short_description: "Repo-specific patterns and workflows for homeassconfig"
  default_prompt: "Use the homeassconfig repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true